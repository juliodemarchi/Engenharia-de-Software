#include <stdio.h>
#include <stdlib.h>

typedef struct {
     char nome[30];
     int idade:
} Pessoa;

#define MAX 5
typedef struct {
Pessoa itens[MAX];
int inicio;
int fim;
int total;
} Fila;

int main() {
  return 0;
}
